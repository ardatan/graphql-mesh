merger: bare
sources:
  - name: Pets
    handler:
      openapi:
        source: ./openapi.json
        endpoint: http://127.0.0.1:{env.PETS_UPSTREAM_PORT}
    transforms:
      - extend:
          typeDefs: ./extensions/typeDefs.graphql
          resolvers: ./extensions/resolvers.ts
