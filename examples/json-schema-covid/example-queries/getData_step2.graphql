# STEP2: 2 sources combined
query getData_step2 {
  case(countryRegion: "France") {
    confirmed
    deaths
    population {
      records {
        fields {
          value
        }
      }
    }
  }
}
