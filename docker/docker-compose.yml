version: '3.8'

services:
  graphql-mesh:
    image: theguild/graphql-mesh-v1
    ports:
      - '4000:4000'
    environment:
      HIVE_CDN_ENDPOINT: SECRET
      HIVE_CDN_KEY: SECRET
      HIVE_REGISTRY_TOKEN: SECRET
