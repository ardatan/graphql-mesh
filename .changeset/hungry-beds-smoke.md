---
'@graphql-mesh/fusion-composition': patch
---

Assign `Query.typenamesByKeysList` semantic convention as the entity resolver.

Previously it only assigned `Query.typenamesByKeys` semantic convention for batched type resolutions
