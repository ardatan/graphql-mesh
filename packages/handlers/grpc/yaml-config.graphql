type GrpcHandler implements Handler {
  name: String!
  config: GrpcHandlerConfig!
}

type GrpcHandlerConfig {
  endpoint: String!
  protoFilePath: String!
  serviceName: String!
  packageName: String!
}
