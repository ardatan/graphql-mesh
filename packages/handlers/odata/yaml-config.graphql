extend type Handler {
  oData: ODataHandler
}

type ODataHandler {
  baseUrl: String!
  operationHeaders: JSON
  schemaHeaders: JSON
  services: [ODataServiceConfig]
}

type ODataServiceConfig {
  name: String!
  headers: JSON
}
