extend type Handler {
  oData: ODataHandler
}

type ODataHandler {
  baseUrl: String!
  operationHeaders: JSON
  metadataHeaders: JSON
  services: [ODataServiceConfig!]!
}

type ODataServiceConfig {
  servicePath: String!
  operationHeaders: JSON
  metadataHeaders: JSON
}
