# Apollo GraphOS

`TODO: Add managed federation and reporting docs here`

## How to use?

`TODO: Mention config is not needed when env vars are used as in Apollo Router/Gateway`

```ts filename="mesh.config.ts"
import { defineConfig as defineServeConfig } from '@graphql-mesh/serve-cli'

export default defineServeConfig({
  graphos: {
    // The API key provided by Apollo GraphOS
  }
})

;`TODO: Persisted Operations`
```
